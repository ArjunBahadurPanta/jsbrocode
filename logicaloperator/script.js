// Logical operator = used to combine or manipulate boolean values (true or false)
// AND = &&
// OR = ||
// NOT = !

// const temp = 200;

// if (temp > 0 && temp <= 30) {
//   console.log("Weather is good.")
// }

// else {
//   console.log("Weather is not good.")
// }

// if (temp < 0 || temp > 30) {
//   console.log("Weather is not good.")
// }

// else {
//   console.log("Weather is  good.")
// }



// ------Strict equality------- //

// = assignment operator
// == comparison operator (compare if values are equal(not data type))
// === strict equality operator (compare if both values and datatype are equal)
// != inequality operator
// !== strict inequality operator

const PI = 3.14159;

if (PI = "3.14159") {
  console.log(PI)
}
else {
  console.log("It is not PI")
}

